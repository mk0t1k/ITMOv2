# R.C.T.F.

- **Role:** SRE/QA, без выдуманного SLA
- **Context:** POST /api/reviews. API-1 = 413 при diff>20000. REL-1 = timeout 10с. RPS/p95 в CASE.md нет. Одиночные 413/timeout уже в integration/e2e. tests_load.md сам себе противоречит.
- **Task:** Исправь tests_load.md: убери неподтверждённые пределы, не дублируй одиночные тесты, согласуй таблицу с условием запуска.
- **Format:** тот же markdown: таблица + обоснование. Без новых ##.

## Полный запрос

```text
Role: SRE/QA. Не выдумывай SLA.
Context: POST /api/reviews. API-1 = 413 при diff>20000. REL-1 = timeout 10с. RPS/p95 в CASE.md нет. Одиночные 413/timeout уже в integration/e2e. tests_load.md сам себе противоречит.
Task: исправь tests_load.md: убери неподтверждённые пределы, не дублируй одиночные тесты, согласуй таблицу с условием запуска.
Format: тот же markdown — таблица + обоснование. Без новых ##.
Верни готовый tests_load.md. Вставь текущий файл.
```

## Что получили

Та же идея, что у few-shot, но явно: «не дублировать одиночные», «RPS/p95 не фиксируем, их нет в CASE.md», условие расширенного профиля — внешний LLM + формальное SLA.
https://opncd.ai/share/GxYCdx98

## Что изменили в исходном артефакте

- Файл и раздел: [`rctf/tests_load.md`](tests_load.md), таблица + обоснование
- Изменение: конкурентные API-1 и REL-1; убраны 10 RPS, p95, 1%
- Как проверили: сверка с API-1/REL-1 и с одиночными строками integration/e2e
- Что отклонили: новые ##; новые SLA-числа
